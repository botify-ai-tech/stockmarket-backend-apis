version: "3.8"

services:
  app:
    build:
      context: .
    container_name: backend-app
    command: uvicorn server.main:app --host 0.0.0.0 --port 8000 --reload
    env_file:
      - .env
    ports:
      - "7000:8000"

